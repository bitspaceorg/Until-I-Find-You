//! Bounding-box tracker. Supports axis-aligned and oriented boxes.
//! Geometry: `(SE(2), w, h)` for oriented; `(x, y, w, h)` for AA.
//! TODO: implement.

#![forbid(unsafe_op_in_unsafe_fn)]

/// Placeholder.
pub struct BBoxTracker;
